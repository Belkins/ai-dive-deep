---
import ThemeToggle from './ThemeToggle.astro';
import { CHANGELOG } from '@/lib/changelog';
import { CHAPTERS } from '@/lib/chapters';
const edition = CHANGELOG[0].edition;
const chapterCount = CHAPTERS.length;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');

// The menu exposes the full navigation at every width; desktop links are a shortlist.
const mobileNav = [
  { group: 'Start', links: [
    { href: '/day-zero/', label: 'Start here' },
    { href: '/learn/', label: 'Learn' },
    { href: '/how-to-read/', label: 'How to read' },
  ]},
  { group: 'Read', links: [
    { href: '/library/', label: 'Library + learning paths' },
    { href: '/journey/', label: 'Journey' },
    { href: '/questions/', label: 'Questions' },
    { href: '/sections/', label: 'Sections' },
  ]},
  { group: 'Reference', links: [
    { href: '/workflow-planner/', label: 'Workflow planner' },
    { href: '/radar/', label: 'Radar' },
    { href: '/opus-5/', label: 'Opus 5' },
    { href: '/fable-5/', label: 'Fable 5' },
    { href: '/showcase/', label: 'Setup' },
    { href: '/resources/', label: 'Resources' },
    { href: '/cheat-sheet/', label: 'Cheat sheet' },
    { href: '/glossary/', label: 'Glossary' },
    { href: '/tier-list/', label: 'Tier list' },
  ]},
  { group: 'More', links: [
    { href: '/cad-as-code/', label: 'CAD-as-code' },
    { href: '/music-is-math/', label: 'Music is Math' },
    { href: '/good-taste/', label: 'Good taste' },
    { href: '/claude-code-best-practices/', label: 'CC best practices' },
    { href: '/claude-code-vs-cursor/', label: 'CC vs Cursor' },
    { href: '/claude-code-vs-codex/', label: 'CC vs Codex' },
    { href: '/claude-code-pricing/', label: 'CC pricing' },
    { href: '/claude-code-hooks/', label: 'CC hooks' },
    { href: '/claude-code-mcp/', label: 'CC + MCP' },
    { href: '/ai-agent-examples/', label: 'Agent examples' },
    { href: '/what-is-agentic-coding/', label: 'Agentic coding' },
    { href: '/build-vs-buy/', label: 'Build vs Buy' },
    { href: '/cfo-case/', label: 'For your CFO' },
    { href: '/claude-md-rules/', label: 'CLAUDE.md rules' },
    { href: '/cowork-setup/', label: 'Cowork setup' },
    { href: '/vault-starter/', label: 'Vault starter' },
    { href: '/weekend-builds/', label: 'Weekend builds' },
    { href: '/starter-skills/', label: 'Starter skills' },
    { href: '/thirty-day-plan/', label: '30-day plan' },
    { href: '/about/', label: 'About' },
  ]},
];
---
<header class="sticky top-0 z-40 backdrop-blur border-b" style="border-color: rgb(var(--line)); background: rgb(var(--bg) / 0.78);">
  <nav class="container-wide flex items-center justify-between h-14">
    <a href={`${base}/`} class="flex items-center gap-2 group shrink-0" style="color: rgb(var(--fg))">
      <span class="font-display text-lg tracking-tight font-medium whitespace-nowrap">Vlad's Playbook</span>
      <span class="hidden xl:inline text-xs px-1.5 py-0.5 rounded" style="background: rgb(var(--line)); color: rgb(var(--muted))">{edition}</span>
    </a>
    <div class="flex items-center gap-0.5 sm:gap-1.5 min-w-0">
      <a href={`${base}/day-zero/`} class="hidden lg:inline-flex whitespace-nowrap text-sm px-2.5 py-2 rounded hover:bg-white/5 transition" style="color: rgb(var(--accent))">Start here</a>
      <a href={`${base}/library/`} class="hidden lg:inline-flex whitespace-nowrap text-sm px-2.5 py-2 rounded hover:bg-white/5 transition" style="color: rgb(var(--accent-2))">Library</a>
      <a href={`${base}/radar/`} class="hidden lg:inline-flex whitespace-nowrap text-sm px-2.5 py-2 rounded hover:bg-white/5 transition" style="color: rgb(var(--accent-2))">Radar</a>
      <a href={`${base}/workflow-planner/`} class="hidden xl:inline-flex whitespace-nowrap text-sm px-2.5 py-2 rounded hover:bg-white/5 transition" style="color: rgb(var(--fg) / 0.85)">Workflow planner</a>
      <span
        id="read-counter"
        data-chapter-total={chapterCount}
        class="hidden items-center gap-1.5 text-xs px-2 py-1 rounded font-mono whitespace-nowrap shrink-0"
        style="border: 1px solid rgb(var(--accent-2) / 0.4); background: rgb(var(--accent-2) / 0.08); color: rgb(var(--accent-2));"
        aria-label="Reading progress"
        title="Chapters you've scrolled to ≥85% of. Stored locally in your browser."
      >
        <svg viewBox="0 0 24 24" class="h-3 w-3" fill="none" stroke="currentColor" stroke-width="3"><path d="M5 12l5 5L20 7" stroke-linecap="round" stroke-linejoin="round"/></svg>
        <span id="read-counter-text">0 / {chapterCount}</span>
      </span>
      <button
        type="button"
        id="open-palette-btn"
        class="inline-flex items-center justify-center gap-2 text-xs rounded min-w-[44px] min-h-[44px] sm:min-w-[130px] px-2 sm:px-2.5 py-1.5"
        style="border: 1px solid rgb(var(--line)); color: rgb(var(--muted)); background: rgb(var(--bg) / 0.4)"
        aria-label="Search (Cmd-K)"
        title="Search (⌘K / Ctrl-K)"
      >
        <svg viewBox="0 0 24 24" class="h-3.5 w-3.5 shrink-0" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3" stroke-linecap="round"/>
        </svg>
        <span class="hidden sm:inline flex-1 text-left">Search…</span>
        <kbd id="palette-kbd-hint" class="hidden sm:inline-flex items-center gap-0.5 text-[10px] px-1 py-0 rounded font-mono" style="border: 1px solid rgb(var(--line)); color: rgb(var(--muted))">⌘K</kbd>
      </button>
      <ThemeToggle />
      <button
        type="button"
        id="mobile-menu-btn"
        class="inline-flex items-center justify-center rounded p-2 min-w-[44px] min-h-[44px]"
        style="border: 1px solid rgb(var(--line)); color: rgb(var(--fg))"
        aria-label="Open menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 6h16M4 12h16M4 18h16" stroke-linecap="round"/></svg>
      </button>
    </div>
  </nav>

  <!-- The full navigation remains available alongside the desktop shortlist. -->
  <div id="mobile-menu" class="hidden border-t" style="border-color: rgb(var(--line)); background: rgb(var(--bg))">
    <div class="container-wide py-4 max-h-[calc(100dvh-3.5rem)] overflow-y-auto">
      {mobileNav.map((g) => (
        <div class="mb-4 last:mb-1">
          <div class="text-[0.62rem] uppercase tracking-[0.22em] mb-1.5 px-2" style="color: rgb(var(--muted))">{g.group}</div>
          <div class="grid grid-cols-2 gap-1">
            {g.links.map((l) => (
              <a href={`${base}${l.href}`} class="block px-2.5 py-2 rounded text-sm hover:bg-white/5 transition no-underline" style="color: rgb(var(--fg) / 0.9)">{l.label}</a>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</header>

<script>
  import { CHAPTERS } from '@/lib/chapters';
  import { readReadingProgress, READ_THRESHOLD, PROGRESS_CHANGED_EVENT } from '@/lib/reading-progress';
  const chapterSlugs = new Set(CHAPTERS.map(chapter => chapter.slug));
  const closeMenu = (restoreFocus = false) => {
    document.getElementById('mobile-menu')?.classList.add('hidden');
    const button = document.getElementById('mobile-menu-btn');
    button?.setAttribute('aria-expanded', 'false');
    button?.setAttribute('aria-label', 'Open menu');
    if (restoreFocus) button?.focus();
  };
  // Delegation survives Astro's replacement of the header on navigation.
  document.addEventListener('click', event => {
    const target = event.target instanceof Element ? event.target : null;
    if (target?.closest('#open-palette-btn')) {
      closeMenu();
      window.dispatchEvent(new CustomEvent('open-palette'));
    }
    if (target?.closest('#mobile-menu-btn')) {
      const menu = document.getElementById('mobile-menu');
      const button = document.getElementById('mobile-menu-btn');
      const isOpen = menu?.classList.toggle('hidden') === false;
      button?.setAttribute('aria-expanded', String(isOpen));
      button?.setAttribute('aria-label', isOpen ? 'Close menu' : 'Open menu');
    } else if (target?.closest('#mobile-menu a') || !target?.closest('header')) closeMenu();
  });
  document.addEventListener('keydown', event => {
    if (event.key === 'Escape' && document.getElementById('mobile-menu-btn')?.getAttribute('aria-expanded') === 'true') closeMenu(true);
  });

  const decorateReadState = () => {
    try {
      const data = readReadingProgress();
      const readSlugs = new Set(
        Object.entries(data).filter(([slug, pct]) => chapterSlugs.has(slug) && pct >= READ_THRESHOLD).map(([slug]) => slug)
      );

      document.querySelectorAll<HTMLElement>('[data-chapter-slug]').forEach((el) => {
        const slug = el.dataset.chapterSlug;
        if (slug && readSlugs.has(slug)) el.setAttribute('data-read', 'true');
        else el.removeAttribute('data-read');
      });

      const counter = document.getElementById('read-counter');
      const counterText = document.getElementById('read-counter-text');
      if (counter && counterText) {
        const total = CHAPTERS.length;
        if (readSlugs.size > 0) {
          counter.classList.add('sm:inline-flex');
          counterText.textContent = `${readSlugs.size} / ${total}`;
        } else {
          counter.classList.add('hidden');
          counter.classList.remove('sm:inline-flex');
        }
      }
    } catch {}
  };
  decorateReadState();
  document.addEventListener('astro:page-load', () => {
    closeMenu();
    decorateReadState();
    const hint = document.getElementById('palette-kbd-hint');
    if (hint && !/Mac|iPhone|iPad/.test(navigator.platform)) hint.textContent = 'Ctrl K';
  });
  // Re-decorate after returning from a chapter (focus/visibility transition)
  window.addEventListener('focus', decorateReadState);
  document.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'visible') decorateReadState();
  });
  // Allow other pages (e.g., Footer reset link) to broadcast a refresh
  window.addEventListener(PROGRESS_CHANGED_EVENT, decorateReadState);
  window.addEventListener('storage', decorateReadState);
</script>
