SCREENSHOTS — what's left (updated 2026-05-18, post Tier-B SVG sweep)

This folder holds 0-byte placeholders for shots not yet captured.
Drop a real PNG into public/screens/ (one level UP, not here) named the
exact ID — e.g. 23-vibe-coding-2.png — and the build picks it up. Or
paste it in chat with the ID. Your original is COPIED, never moved.

────────────────────────────────────────────
STATUS: VISUALLY COMPLETE — 2 OPTIONAL PHOTOS LEFT
────────────────────────────────────────────
63 visual artifacts done (46 SVG + 17 real PNG) · 2 captures still open.

  ★ TIER S — CLEARED ✓
  ★ TIER A — CLEARED ✓
  ★ TIER B — CLEARED ✓ (20 rendered as sanitized SVGs)

Every chapter placeholder now renders. The only 2 open slots are
deliberately left as real device photos — a mock would undercut the
"this actually happened on a real phone" point both make:

   19-build-products-2   Phone screen: deployed URL playing the voice
                         memo, timestamp visible (the 1 AM-bug evidence)
   23-vibe-coding-2      Phone screen: 2026-05-09.mp3 playing / iCloud
                         folder synced ("this is the moment" payoff)

These are not blocking. The chapters read fine without them. If/when
you want them, take the real screenshot and drop it in public/screens/
(copy-not-move applies — your original stays wherever you put it).

────────────────────────────────────────────
DONE — 63 (46 SVG diagrams + 17 real PNG captures)
────────────────────────────────────────────
Source of truth: src/lib/screenshots.ts (auto-generated by
scripts/scan-screenshots.py on every build).

This sweep added 20 sanitized Tier-B SVGs in 4 swarm-researched
batches (ch 03/06/09/10/11 · 15/16/17/18 · 19/20/23 · 27/33/34/35),
each numerically/structurally faithful to the chapter prose it sits in.

────────────────────────────────────────────
HOW TO DROP THE LAST 2
────────────────────────────────────────────
1. Take the screenshot on the phone (real device — that's the point).
2. Sanitize: no private contacts, tokens, or account-identifying data.
3. Name it exactly 19-build-products-2.png or 23-vibe-coding-2.png
4. Drop in public/screens/ (one level UP) → `npm run build` or push.
   OR paste in chat with the ID — it gets filed, your copy stays put.

────────────────────────────────────────────
SANITIZATION CHECKLIST
────────────────────────────────────────────
- Mask contact/customer/deal/channel names → generic
- Mask OAuth tokens, API keys, session URLs, signed S3 URLs
- Mask dollar amounts that identify a specific account
- When in doubt, over-sanitize — this is a public artifact.
